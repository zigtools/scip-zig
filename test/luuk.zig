const std = @import("std");
pub const loris = @import("loris.zig");

pub fn main() void {
    loris.myFunc();
    loris.king.playsFortnite();

    std.base64.Base64Decoder.init(0, 0);
    std.crypto.aead.aegis.Aegis128L.decrypt(0, 0, 0, 0, 0, 0);
}
